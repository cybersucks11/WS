<?php
$conn = pg_connect("host=localhost dbname=test user=postgres password=postgres");

/*
CREATE TABLE users(
id SERIAL PRIMARY KEY,
name VARCHAR(100),
contact VARCHAR(15),
email VARCHAR(100),
password TEXT
);
*/
?>
